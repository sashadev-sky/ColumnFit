# ColumnFit
A Google Sheets macro that resizes every row to fit the tallest wrapped text across 1 or more selected columns, ignoring the height demands of all other columns, by estimating line counts from font metrics &amp; column width since Sheets exposes no per-column auto-fit.
